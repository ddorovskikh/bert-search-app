apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pm-k8s-config.fullname" . }}-test-connection"
  labels:
    {{- include "pm-k8s-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pm-k8s-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
